# Google OAuth — OPTIONAL.
# Only needed to enable POST /api/auth/google.
# All other endpoints work without this; docker compose up works with no .env file at all.
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
